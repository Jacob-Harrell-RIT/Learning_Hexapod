add wiring diagram
add BOM
add build instructons
  add instructions for servo modification to encoder
add multiple encoders
add ssc32 servo control
add STL and CAD files for servo holders and cases
add GA to pi
up/down encoders

